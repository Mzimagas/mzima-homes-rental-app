# Run lint-staged to check and fix staged files
npx lint-staged

# Run type checking
npm run typecheck

# Run tests on staged files
npm run test:staged 2>/dev/null || echo "No test:staged script found, skipping..."
